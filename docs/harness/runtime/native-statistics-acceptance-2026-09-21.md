# 统计与独立飞书网关迁移验收（2026-09-21）

## 交付状态

- 服务器实现 `ec78b35`，已部署为 `20260921T084109Z-ec78b35a42aa`，Agent/ERP 健康检查均为 ok。旧鉴权入口保留。
- 桌面业务实现 `06ce8c1a`；随后 `b570c0d8` 只修正 Windows Bun 流式错误测试的断言方式，生产代码未变。
- 独立网关最初实现 `9da9fa8`，随后 `e45b335` 撤除不必要的公司 MCP 本机转发，恢复 Codex 直接连接；只在本机仓库提交，没有推送、部署或重启 Mac mini，没有自动发送飞书消息。

## 验证结果

| 范围 | 结果 |
|---|---|
| 服务器全量 | 1415 passed，无数据库测试跳过；专用本机 PostgreSQL 库 `lxe_stats_device_test` |
| 服务器构建 | Agent/Admin/ERP 三套前端构建通过 |
| 桌面 Mac 完整源码验证 | 类型/协议/边界检查通过；Bun 1659 passed、3 skipped；Python 1764 passed、2 skipped、54 subtests passed |
| 桌面构建 | Mac、Windows 开发产物均通过；没有生成安装包 |
| Windows 定向 | 6 个文件分进程合计 75 passed、0 failed；类型检查通过 |
| Windows 原生上传 | 真实本机 HTTP + SQLite 验证无 Token 请求、503 后相同批次续传、有效 ACK 推进游标、无效/超限 ACK 不推进、409 暂停 |
| Windows 线上只读 | 移除业务 Token 后 `lxeskill cloud-context --server http://10.88.0.1:8000` 退出 0 |
| 飞书网关 | 类型检查、构建通过；全量 646 passed、3 skipped |
| 飞书 MCP | 真实项目 Codex SDK + 本机模拟模型/上游完成初始化、列工具、工具调用；固定转发层拒绝重定向；浏览器请求、串改路径、Cookie、流式取消均有测试 |

Mac Bun 的 3 个跳过属于 Windows 平台场景，Python 有 2 个可选场景跳过；网关的 3 个跳过是显式开关控制的 XLSX 渲染/大表回读，不算通过。Python 测试退出成功，但结束时仍有 aiohttp 未关闭会话的诊断，本轮未修改 Python 实现。

Windows 最初在 MCP 流式错误的异步 rejects matcher 处发生 Bun 1.4.2 原生崩溃。测试改为显式 await/catch 并断言同一实际错误后，Mac 和 Windows 定向测试通过；拒绝、取消与真实错误检查均保留。

隔离 Windows Electron 加载 `app://lxe/` 首次设置页面并截图成功。该开发模式的公司接入页面按现有限制显示“平台不支持”，因此本轮没有通过 GUI 激活真实设备或完成真实账号聊天；上传链路由上述真实 HTTP/SQLite 与主进程配置测试验收。没有改动用户正式桌面配置。

## 历史归属与后续发布

上述表格保留最初迁移的验证记录。后续核实发现，Codex 0.154.0 已内置跨源重定向限制；最初把允许同源路径跳转误当成可以访问任意服务器的证据，因此增加了不必要的转发层。`e45b335` 已撤除该层，保留设备鉴权、飞书用户授权、工具限制和代理绕过。纠正后类型检查、构建通过，全量 647 passed、3 skipped；实际 SDK 隔离测试验证直连和同源跳转均完成 MCP 调用，跨源 307/308 的目标收到 0 次请求，观察到的请求没有 Authorization/Cookie。服务器与桌面代码不需要为此修改，Mac mini 尚未发布。

生产只做了健康/权限查询和历史归属汇总读取，没有写入模拟统计、停用设备或收权。已发现独立网关 631 条历史统计均未验证设备归属。旧版本可继续运行；未来更新 Mac mini 前，必须核实它的持久化 machine_id 并按服务器维护命令登记归属，然后预览/应用网关配置迁移。不能自动猜测或新建 ID 绕过。

旧业务 Token 申请/刷新和旧服务端鉴权分支本轮保留。当前结果是迁移后的日常调用不再依赖业务 Token，不代表所有旧凭据已经删除。
